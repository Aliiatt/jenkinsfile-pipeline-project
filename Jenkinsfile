pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself cheeeeck  yesss it worked'
                sh 'python3 --version'
                sh 'python3 pipeline.py'
            }
        }
    }
}
